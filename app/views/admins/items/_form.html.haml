-# frozen_string_literal: true
= simple_form_for [:admins, item] do |f|
  .form-inputs
    = f.input :image
    = f.input :name
    = f.input :description
    = f.input :price
  .form-actions
    = f.button :submit
