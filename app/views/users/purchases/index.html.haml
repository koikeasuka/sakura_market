%h2 購入履歴
%table.table
  %thead
    %tr
      %th= Purchase.human_attribute_name(:created_at)
      %th= Purchase.human_attribute_name(:total_price)
  %tbody
    - @purchases.each do |purchase|
      %tr
        %td= link_to (l purchase.created_at, format: :long), users_purchase_path(purchase),
          class: 'link-dark link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover ms-2'
        %td= number_to_currency(purchase.total_price)

